name: dev

on:
  push:
    branches: [dev]

jobs:
  Build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@main

      - name: Build container
        run: docker build . -t 5scontrol/machine_control_js:latest

      - name: Push container
        run: docker push 5scontrol/machine_control_js:latest
